// DEPRECATED — ancienne version inutilisée de CoursesShell.tsx (aucune page ne l'importe).
// Conservé (ré-export) car la suppression automatique n'était pas possible ; peut être supprimé manuellement.
export { default } from './CoursesShell'
